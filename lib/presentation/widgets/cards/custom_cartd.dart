import 'package:flutter/material.dart';

class CustomCartd extends StatelessWidget {
  const CustomCartd({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
